---
description: Compare user's solution with an external solution
---

Follow **Rule 3 (Compare)** from `.cursor/rules/dsa-workflow.mdc`.

The user will provide an external solution either as code in the file or directly in the prompt.
